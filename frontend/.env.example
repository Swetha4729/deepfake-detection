# Vercel: point the frontend at the deployed Render backend (no '/' at the end).
VITE_API_URL=https://voiceguard-backend.onrender.com

# For local dev, comment the line above — the Vite proxy handles it automatically.
